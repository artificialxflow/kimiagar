# TODO v2 - مدیریت درخواست‌های خرید و اطلاع‌رسانی به ادمین

## 🎯 هدف
اطمینان از این‌که هر زمان کاربر درخواست خرید ثبت می‌کند، ادمین به‌صورت واضح و سریع مطلع شود، بتواند سفارش را بررسی کند، قیمت را تایید/رد کند و در صورت لزوم سفارش را لغو نماید.

## ✅ نیازهای کاربر (بر اساس توضیحات)
- کاربر قیمت را می‌بیند و درخواست خرید ثبت می‌کند.
- ادمین باید **فورا مطلع** شود (پیام/صدا/نوتیفیکیشن).
- تایمر **۱۸۰ ثانیه‌ای** برای رزرو قیمت نمایش داده شود.
- ادمین بتواند وضعیت سفارش را تایید، رد، یا «خرید انجام نشد» ثبت کند.
- اگر قیمت بازار تغییر کرد، امکان ثبت «معامله بسته نشد» وجود داشته باشد.
- همه‌ی این‌ها **بدون اتصال مستقیم به API خرید طلا** انجام شود (خرید واقعی را ادمین انجام می‌دهد).

---

## 🔧 Backend / API
- [ ] ایجاد/تکمیل API برای **اطلاع‌رسانی سفارش جدید** (ایجاد نوتیفیکیشن/لاگ)
- [ ] اضافه کردن فیلد/وضعیت جدید برای سفارش‌ها (مثلاً `REJECTED_PRICE_CHANGE` یا «معامله بسته نشد»)
- [ ] اطمینان از اینکه endpoint تایید سفارش، امکان ثبت نتیجه‌ی متفاوت (تایید، رد، لغو به‌خاطر قیمت) را دارد
- [ ] اضافه کردن API برای **خواندن سفارش‌های جدید/در انتظار** برای ادمین
- [ ] ثبت timestamp شروع تایمر (برای نمایش باقی‌مانده ۱۸۰ ثانیه)

## 🖥️ Frontend / Admin Panel
- [ ] نمایش **لیست سفارش‌های جدید** با وضعیت «در انتظار تایید»
- [ ] نمایش **تایمر ۱۸۰ ثانیه‌ای** برای هر سفارش (countdown)
- [ ] نمایش اطلاعات سفارش (نام کاربر، مبلغ، قیمت، زمان باقی‌مانده)
- [ ] نمایش دکمه‌های **تایید، رد، لغو به دلیل تغییر قیمت**
- [ ] نمایش پیام خاص اگر قیمت تغییر کرده و سفارش نامعتبر شده
- [ ] نمایش badge/notification واضح در پنل ادمین هنگام ثبت سفارش جدید

## 🔔 اطلاع‌رسانی
- [ ] ارسال نوتیفیکیشن بلادرنگ به ادمین (مثلاً toast، modal، badge در header)
- [ ] اضافه کردن **صدا یا انیمیشن** هنگام ورود سفارش جدید (اختیاری، ولی تأکید شده)
- [ ] اطمینان از اینکه ادمین حتی در تب دیگر هم متوجه سفارش می‌شود (مثلاً title blinking)

## ⏱️ تجربه کاربری کاربر
- [ ] نمایش تایمر ۱۸۰ ثانیه‌ای در UI کاربر بعد از ارسال سفارش
- [ ] نمایش پیام «در حال انجام معامله، لطفاً صبور باشید» (تأکید شده)
- [ ] امکان لغو سفارش توسط کاربر تا قبل از تایید ادمین (در صورت نیاز)

## 📊 مدیریت وضعیت سفارش
- [ ] افزودن وضعیت‌های جدید: `PENDING`, `EXPIRED`, `REJECTED`, `REJECTED_PRICE_CHANGE`, `COMPLETED`
- [ ] منطق خودکار برای تغییر وضعیت به `EXPIRED` بعد از ۱۸۰ ثانیه اگر ادمین تصمیم نگرفت
- [ ] نمایش تاریخچه تصمیمات ادمین (چه کسی تایید/رد کرد، دلیل رد)

## 📣 پیام‌ها و محتوا
- [ ] متن واضح برای اطلاع‌رسانی به ادمین: «کاربر X درخواست خرید Y تومان طلا داده است»
- [ ] متن مناسب برای کاربر در صورت تایید، رد، یا تغییر قیمت
- [ ] پیام «معامله بسته نشد» با توضیح دلیل (مثلاً: «قیمت تغییر کرد»)

## ✅ تست‌ها و سناریوها
- [ ] تست ثبت سفارش توسط کاربر و اطلاع‌رسانی بلادرنگ به ادمین
- [ ] تست شمارش معکوس ۱۸۰ ثانیه‌ای و تغییر وضعیت بعد از آن
- [ ] تست تایید/رد/لغو با قیمت تغییر کرده
- [ ] تست صدای هشدار و نوتیفیکیشن در پنل ادمین
- [ ] تست UI کاربر هنگام انتظار تایید

---

**وضعیت:** در انتظار اجرا  
**اهمیت:** حیاتی برای تحویل کامل سیستم به ادمین (مطابق درخواست panel.md)  

